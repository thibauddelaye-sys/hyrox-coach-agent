# Hyrox Coach Agent — Python dependencies
# Install with:  pip install -r requirements.txt

# RAG ingestion + retrieval
openai>=1.30.0
pinecone-client>=4.0.0
cohere>=5.0.0
tiktoken>=0.7.0

# Airtable client (used by ingest scripts and dashboard)
pyairtable>=2.3.0

# Dashboard
streamlit>=1.35.0
pandas>=2.2.0
altair>=5.3.0

# Strava token bootstrap (one-shot script)
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0
